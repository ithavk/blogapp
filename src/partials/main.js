// Main Partial (Blog Posts Container)
export function renderMain() {
  return `
    <section class="main__posts row g-4" id="postsContainer">
      <!-- Blog posts will be dynamically rendered here -->
    </section>
  `;
}
